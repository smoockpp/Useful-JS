var answer = prompt('What programming language is the name og a gem?');

if ( answer.toUpperCase() === 'RUBY' ) {
  document.write("<p>That's right</p>");
} else {
  document.write("<p>That's not true!"); 
}